# Frontend API base URL.
# If empty, frontend uses '/api' (works in Vite dev mode via proxy from vite.config.ts).
# For direct backend calls (e.g. Docker/Nginx without reverse proxy), set full URL:
# VITE_API_URL=http://localhost:8080
VITE_API_URL=
