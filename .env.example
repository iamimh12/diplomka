# Frontend API base URL.
# In Docker runtime this value is injected into /runtime-env.js on container start.
# In local Vite dev it is read from import.meta.env.
# If empty, frontend uses '/api' (works in Vite dev mode via proxy from vite.config.ts).
# For direct backend calls set full URL, e.g.:
# VITE_API_URL=http://localhost:8080
VITE_API_URL=
